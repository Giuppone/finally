# FinAlly Project - the Finance Ally

All project documentation is in the `planning` directory.

The key document is PLAN.md included in full below. An earlier market data implementation was deliberately deleted so it can be rebuilt from scratch — `backend/` is currently empty and no working price cache exists yet. Design notes from that first pass are kept in the `planning/archive` folder for reference; consult them only when required. The whole platform, market data included, is still to be developed.

Section 13 of PLAN.md is a decision record resolving open questions from a doc review — those decisions are already reflected in sections 1–12.

@planning/PLAN.md